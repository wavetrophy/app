import { GeoDirective } from './geo.directive';

describe('GeoDirective', () => {
  it('should create an instance', () => {
    const directive = new GeoDirective();
    expect(directive).toBeTruthy();
  });
});
